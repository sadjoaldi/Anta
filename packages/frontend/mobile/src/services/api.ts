// const API_URL = process.env.EXPO_PUBLIC_API_URL || 'http://192.168.1.100:4000';

// export type HealthResponse = { ok: boolean; service?: string; time?: string; error?: string };

// export async function ping(): Promise<HealthResponse> {
//   try {
//     const res = await fetch(`${API_URL}/api/health`);
//     return res.json();
//   } catch (e: any) {
//     return { ok: false, error: e?.message };
//   }
// }

// export default { ping };
